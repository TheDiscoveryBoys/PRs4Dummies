# GPU version for Google Colab or GPU machines
# Install with: pip install -r requirements-gpu.txt

# Core dependencies
langchain>=0.1.0
langchain-community>=0.0.10
sentence-transformers>=2.2.0
tqdm>=4.64.0
python-dotenv>=1.0.0
transformers>=4.30.0
numpy>=1.24.0

# GPU-accelerated FAISS (much faster on GPU)
faiss-gpu>=1.7.4

# PyTorch with CUDA support (auto-detects if you have CUDA)
torch>=2.0.0

# Optional: For Google Colab (automatically available in Colab)
# google-colab
